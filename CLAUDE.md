# Claude Code Instructions

Use the `rust-performance-engineering` skill for Rust performance work. Use specialists such as `rust-python-pyo3-maturin`, `rust-wasm-engineering`, `rust-ffi-bindings`, and `rust-review-auditor` when the task matches them.

If the skill system is unavailable, read `skills/rust-performance-engineering/SKILL.md` and only load specialist skills or reference files needed for the current task.
